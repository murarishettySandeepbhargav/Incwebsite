import React from "react";
import "./Preloader.css"; // Add a separate CSS file for preloader styles

const Preloader = () => {
  return (
    <div className="preloaders">
      <div className="spinner"></div>
      <br></br>
      <p className="Pre">INC Global-Technology & Innovation</p>
    </div>
  );
};

export default Preloader;
